fragment AccordionFields on List {
  sys {
    id
  }
  accordionTitle
}

query CtfAccordion($id: String!, $locale: String, $preview: Boolean) {
  list($id: String, preview: $preview, locale: $locale) {
    ...AccordionFields
  }
}
